sonar.projectKey=noxitylabs_better-cpanel-error-pages
sonar.organization=noxitylabs

# The four error pages share an intentionally identical nav and footer.
# Skip duplication detection on HTML so the brand consistency isn't flagged.
sonar.cpd.exclusions=**/*.html
